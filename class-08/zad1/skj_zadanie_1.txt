s29758 s29758
Twoje hasło to: SvNnmxAoSP
1. W jednej linii otrzymasz od serwera 3 liczby o typie int, odeślij jedną linię zawierającą ich konkatenację (sklejenie). Serwer odeśle informację czy Twoja odpowiedź jest poprawna
-----------------------------------------------------------
In the one line you will receive 3 int numbers from the server. Send back one line containing their concatenation. The server will send back information whether your answer is correct;

2. Od serwera otrzymasz 4 liczby o typie int. Każda liczba w osobnej linii. Odeślij jedną linię zawierającą ich konkatenację (sklejenie). Serwer odeśle informację czy Twoja odpowiedź jest poprawna
-----------------------------------------------------------
From the server you will receive 4 int numbers. Each number in a separate line.  Send back one line containing their concatenation. The server will send back information whether your answer is correct;

3. Od serwera otrzymasz 8 dużych liczb, każda liczba w osobnej linii. Po odebraniu wszystkich 8 linii, odeślij 4 linie, w każdej linii sklejone parami (otrzymane wcześniej) liczby, czyli dla ośmiu wysłanychprzykładowych liczb 1,2,3,4,5,6,7,8 serwer powinien otrzymać 4 linie o zawartości odpowiednio 12 34 56 78. Serwer odeśle informację czy Twoja odpowiedź jest poprawna
-----------------------------------------------------------
You will receive 8 large numbers from the server. Each number in a separate line. After receiving all 8 lines, send back 4 lines, in each line concatenated in pairs (received earlier) numbers, that is, for eight sent sample numbers 1,2,3,4,5,6,7,8 the server should receive 4 lines with the content of 12 34 56 78 respectively. The server will send back information whether your answer is correct;

4. Od serwera dostaniesz 2*K dużych liczb (gdzie K zostanie przesłane pierwszym komunikatem). Każda liczba w osobnej linii. Odsyłąsz serwerowi K linii. Pierwsza linia zawiera konkatenację pierwszej liczby z pierwszej połowy z pierwsza liczbą z drugiej połowy, druga linia zawiera konkatenacje drugiej liczby z pierwszej połowy, z drugą liczbą z drugiej połowy itd... , czyli dla ośmiu przykładowych liczb 1,2,3,4,5,6,7,8 serwer powinien otrzymać 4 linie o zawartości odpowiednio 15 26 37 48. Uwaga: klient ma niezwłocznie odsyłać wyznaczone już wyniki, wpp. nie otrzyma kolejnych liczb. Czyli jak otrzymał już jakąś liczbę z drugiej połowy to konkatenuje i wysyła wynik do serwera, a serwer wysyła kolejną liczbę z drugiej połowy.Serwer odeśle informację czy Twoja odpowiedź jest poprawna
-----------------------------------------------------------
You will receive 2*K large numbers from the server (where K value will be sent in the first message). Each number in a separate line. You send back K lines to the server. The first line contains the concatenation of the first number from the first half with the first number of the second half of lines, the second line contains the concatenation of the second number of the first half, with the second number of the second half, etc.... For the eight example numbers 1,2,3,4,5,6,7,8, the server should receive 4 lines with the content of 15 26 37 48 respectively. Note: the client is to immediately send back the already determined results, wpp. will not receive further numbers. That is, as it has already received a number from the second half it concatenates and sends the result to the server, and the server sends another number from the second half. The server will send back the information if it is correct.

5. Od serwera dostaniesz 2*K dużych liczb, gdzie K nie zostanie nigdy wysłane przez serwer. Jak się domyślić ile liczb zostanie przesłanych do klienta??? Każda liczba w osobnej linii. Odsyłąsz serwerowi K linii. Pierwsza linia zawiera konkatenację pierwszej liczby z pierwszej połowy z pierwsza liczbą z drugiej połowy, druga linia zawiera konkatenacje drugiej liczby z pierwszej połowy, z drugą liczbą z drugiej połowy itd... , czyli dla ośmiu przykładowych liczb 1,2,3,4,5,6,7,8 serwer powinien otrzymać 4 linie o zawartości odpowiednio 15 26 37 48. Uwaga: klient ma niezwłocznie odsyłać wyznaczone  już wyniki, wpp. nie otrzyma kolejnych liczb. Czyli jak otrzymał już jakąś liczbę z drugiej połowy to konkatenuje i wysyła wynik do serwera, a serwer wysyła kolejną liczbę z drugiej połowy.Serwer odeśle informację czy Twoja odpowiedź jest poprawna
-----------------------------------------------------------
You will receive 2*K large numbers from the server (where K value will never be sent by the server).  How do you guess how many numbers will be sent to the client????  Each number in a separate line. You send back K lines to the server. The first line contains the concatenation of the first number from the first half with the first number of the second half of lines, the second line contains the concatenation of the second number of the first half, with the second number of the second half, etc.... For the eight example numbers 1,2,3,4,5,6,7,8, the server should receive 4 lines with the content of 15 26 37 48 respectively. Note: the client is to immediately send back the already determined results, wpp. will not receive further numbers. That is, as it has already received a number from the second half it concatenates and sends the result to the server, and the server sends another number from the second half. The server will send back the information if it is correct.

